# V840 Login Forms Autocomplete QA

Login cliente, registro y admin-login usan `autocomplete=on`, `autocomplete=username`, `current-password` o `new-password` seg?n corresponda. Labels enlazados con `for/id` y textos corregidos.
