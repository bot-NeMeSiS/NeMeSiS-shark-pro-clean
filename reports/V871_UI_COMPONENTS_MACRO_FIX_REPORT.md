# V871 UI Components Macro Fix Report

## Macros revisadas
- `action_button`.
- `reference_action_button`.
- `pick_card`.
- `reference_pick_card`.
- `v870_reference_widget`.
- `v870_admin_workbench`.
- `v870_client_workbench`.

## Correcciones
- `Pick en revisión` reemplaza texto roto.
- `Requiere sincronización real` reemplaza texto roto.
- `Acción pendiente` y `Revisión segura` reemplazan texto roto.
- Botones usan `aria-label` opcional sin duplicar texto visible.
- Se unifica clase `v871-action-clean` para botones compactos.

## Riesgo
Sin cambios de datos. Solo presentación/copy/macros.
