# V848 Connected Routes Buttons QA

Rutas/enlaces revisados:

- Cliente: `/app`, `/partidos`, `/calendar`, `/live`, `/picks`, `/shark`, `/profile`, `/telegram`, `/support`.
- Secundarias: `/favorites`, `/track-record`, `/combis`, `/mercados`, `/highlights`.
- Admin: `/admin/data-center`, `/admin/api-sports`, `/admin/telegram/command-center`, `/admin/shark-ai`, `/admin/daily-automation`.

No se introducen botones muertos nuevos ni `href=""`.

Check: `tools/check_v848_connected_routes_buttons.py`.
