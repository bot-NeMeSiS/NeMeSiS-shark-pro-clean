# V880 UI Copy Visual Fix Sweep

## Fixes seguros

- Cards y empty states más compactos.
- Grids con gaps controlados.
- Tablas envueltas en overflow seguro si existe wrapper.
- Imágenes sin `src` ocultas para evitar iconos rotos.
- Fallbacks visuales de escudos reforzados.
- Admin sin nav/floating cliente.
- Cliente sin command strip admin.

## Copy

Checks bloquean `None/null/undefined`, mojibake común, Stripe operativo falso, OpenAI operativo falso, Telegram filler y claims de apuesta garantizada.

## Pendiente

Browser QA real para medir huecos y overflow con capturas.
