# V833 Full Ecosystem Linking Final Audit

## Cliente

Enlaces principales presentes: `/app`, `/partidos`, `/calendar`, `/live`, `/directo`, `/picks`, `/shark`, `/profile`, `/telegram`, `/support`, `/track-record`, `/favorites`, `/combis`, `/mercados`, `/highlights`.

## Admin

Enlaces principales presentes: `/admin/dashboard`, `/admin/map`, `/admin/daily-automation`, `/admin/automation-os`, `/admin/telegram/command-center`, `/admin/data-center`, `/admin/users`, `/admin/memberships`, `/admin/payments`.

## Criterios

Los checks validan enlaces core, ausencia de `href=""`, logout, rutas deportivas, enlaces admin y bottom nav cliente con cinco destinos.
