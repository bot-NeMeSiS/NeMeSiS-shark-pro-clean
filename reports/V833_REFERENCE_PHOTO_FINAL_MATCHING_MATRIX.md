# V833 Reference Photo Final Matching Matrix

## Landing / Home

Ruta: `/`. Template: `home.html`. Objetivo: hero SHARK premium, CTA claros, texto corto, fondo con brillo. V833 refuerza el sistema visual global y botones.

## Login / Registro

Rutas: `/cliente-login`, `/registro`. Templates: `client_login.html`, `register.html`. Objetivo: panel premium, inputs cómodos, fondo SHARK, móvil sin overflow.

## App / Dashboard

Ruta: `/app`. Template: `client_app_center.html`. Objetivo: centro principal conectado con partidos, directo, picks, SHARK, Telegram, histórico y favoritos.

## Partidos / Calendar

Rutas: `/partidos`, `/calendar`. Template: `calendar.html`. Objetivo: match rows compactas, filtros, escudos, hora Madrid, estado real y detalle.

## Live / Directo

Rutas: `/live`, `/directo`. Template: `live.html`. Objetivo: centro live real, sin inventar minuto/marcador/eventos.

## Picks

Ruta: `/picks`. Template: `picks.html`. Objetivo: cards vendibles, cuota/stake solo reales, Ver partido y SHARK.

## Match Detail

Ruta: `/match/<id>`. Template: `match_detail.html`. Objetivo: header equipos, datos reales, picks relacionados y enlaces a partidos/picks/SHARK.

## SHARK

Rutas: `/shark`, `/shark-core`, `/shark-ai`. Templates: `shark.html`, `shark_core.html`. Objetivo: pantalla estrella, sin floating duplicado, preguntas y acciones.

## Perfil / Telegram / Soporte

Rutas: `/profile`, `/telegram`, `/support`. Templates: `profile.html`, `telegram.html`, `support.html`. Objetivo: cuenta, plan, conexión Telegram, soporte y salida visibles.

## Histórico / Combis / Mercados / Highlights

Rutas: `/track-record`, `/combis`, `/mercados`, `/highlights`. Objetivo: no parecer antiguas, empty states premium y enlaces al ecosistema.

## Admin

Rutas: `/admin/dashboard`, `/admin/daily-automation`, `/admin/telegram/command-center`, `/admin/data-center`, `/admin/users`, `/admin/memberships`, `/admin/payments`. Objetivo: command center sobrio, conectado y sin elementos cliente.

## Pendiente honesto

No se declara pixel-perfect sin screenshots reales. V833 aumenta coherencia, pero la comprobación visual final debe hacerse en navegador real.
