# V852 Rutas y Botones QA

## Revisado
- `/app`, `/partidos`, `/live`, `/picks`, `/shark`, `/telegram`, `/profile`, `/support`.
- Admin conserva API-SPORTS, Telegram y SHARK.

## Resultado
Los cambios V852 no eliminan hrefs existentes ni cambian rutas sensibles.
