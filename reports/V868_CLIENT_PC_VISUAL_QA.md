# V868 Cliente PC Visual QA

Pantallas revisadas: `/`, `/app`, `/partidos`, `/calendar`, `/live`, `/directo`, `/picks`, `/shark`, `/telegram`, `/profile`, `/support`, `/track-record`.

Mejoras aplicadas:

- Dashboard cliente más compacto mediante grids V868.
- Cards de picks/live/app con borde, profundidad y jerarquía común.
- Acciones rápidas y chips más consistentes.
- Empty states conservan estados reales sin inventar datos.

Pendiente honesto: no se declara pixel-perfect porque no se realizó validación visual humana contra capturas de referencia en esta pasada.
