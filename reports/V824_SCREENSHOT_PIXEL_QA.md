# V824 Screenshot Pixel QA

## Estado

No se generaron screenshots en esta ejecucion.

## Motivo

No habia navegador de pruebas conectado a la sesion. Por tanto, no se declara pixel-perfect.

## Recomendacion

Cuando haya navegador disponible, capturar:

- Desktop 1440: `/`, `/cliente-login`, `/app`, `/partidos`, `/live`, `/picks`, `/shark`, `/profile`, `/support`, `/admin/dashboard`.
- Mobile 390: `/app`, `/partidos`, `/live`, `/picks`, `/shark`, `/profile`.

Guardar en `reports/screenshots_v824/`.
