# V863 Auth Sessions Users Real QA

## Render real sin credenciales

- Rutas privadas de cliente redirigen a `/cliente-login`.
- Rutas admin redirigen a `/admin-login`.
- APIs admin devuelven 403 sin sesión.

## No ejecutado

No se creó usuario test y no se inició sesión real porque no se dieron credenciales ni permiso para generar usuarios `qa_v863_test`.
