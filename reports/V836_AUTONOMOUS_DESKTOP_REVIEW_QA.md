# V836 Autonomous Desktop Review QA

## Revisión aplicada

- Cliente desktop conserva rail visual V828 y topbar limpia.
- Bottom nav móvil se oculta en desktop.
- Shell cliente usa ancho máximo controlado.
- Admin conserva rail y dock de command center.
- Admin no muestra floating SHARK ni bottom nav cliente.
- Cards, paneles, sports rows y pick cards reciben borde/glass consistente.

## Pantallas desktop revisadas

- `/`, `/cliente-login`, `/registro`
- `/app`, `/partidos`, `/calendar`, `/live`, `/directo`, `/picks`
- `/shark`, `/profile`, `/telegram`, `/support`
- `/favorites`, `/track-record`, `/combis`, `/mercados`, `/highlights`
- `/admin/dashboard`, `/admin/daily-automation`, `/admin/automation-os`, `/admin/telegram/command-center`, `/admin/data-center`, `/admin/users`, `/admin/memberships`, `/admin/payments`

## Pendiente

Revisión pixel visual final en navegador real para medir distancias exactas con las fotos.
