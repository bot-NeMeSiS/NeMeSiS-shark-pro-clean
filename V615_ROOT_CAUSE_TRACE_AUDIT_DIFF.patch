*** Begin Patch
*** Update File: app.py
@@
-from engines.observability_engine import ensure_observability_schema, observability_summary, record_observability_event, mark_route_check
+from engines.observability_engine import (
+    build_error_id,
+    ensure_observability_schema,
+    latest_observability_errors,
+    mark_route_check,
+    observability_error_detail,
+    observability_summary,
+    record_observability_error,
+    record_observability_event,
+)
+
+import traceback
@@
+def _observability_user_context():
+    ...
+
+def _record_observability_exception(error, *, status_code=500):
+    ...
+    error_id = build_error_id(...)
+    traceback_full = traceback.format_exc()
+    record_observability_error(...)
+
 @app.errorhandler(500)
 def v607_controlled_500(error):
-    _record_http_problem(500, "internal_error", "Error interno controlado", str(error)[:1500])
+    error_id, user_ctx = _record_observability_exception(error, status_code=500)
@@
-    return render_template("error_controlled.html", ...)
+    return render_template("error_controlled.html", error_id=error_id, is_admin=user_ctx.get("is_admin"), ...)
+
 @app.errorhandler(Exception)
 def v607_controlled_exception(error):
-    _record_http_problem(code, "unhandled_exception", "Excepción controlada", str(error)[:1500])
+    error_id, user_ctx = _record_observability_exception(error, status_code=int(code))
@@
+@app.route("/admin/observability/errors")
+def admin_observability_errors_page():
+    ...
+
+@app.route("/api/observability/errors")
+def api_observability_errors():
+    ...

*** Update File: engines/observability_engine.py
@@
+CREATE TABLE IF NOT EXISTS observability_errors (
+    id INTEGER PRIMARY KEY AUTOINCREMENT,
+    error_id TEXT NOT NULL UNIQUE,
+    created_at TEXT NOT NULL,
+    path TEXT,
+    method TEXT,
+    endpoint TEXT,
+    user_id TEXT,
+    email TEXT,
+    membership TEXT,
+    exception_type TEXT,
+    exception_message TEXT,
+    traceback_full TEXT,
+    user_agent TEXT,
+    ip TEXT,
+    referrer TEXT,
+    request_id TEXT,
+    app_version TEXT
+)
+
+def build_error_id(seed: str = "") -> str:
+    ...
+
+def record_observability_error(...):
+    ...
+
+def latest_observability_errors(...):
+    ...
+
+def observability_error_detail(...):
+    ...

*** Update File: templates/error_controlled.html
@@
+{% if error_id %}
+<p class="small">ID de error: <strong>{{ error_id }}</strong></p>
+{% endif %}
+{% if is_admin and error_id and error_id != 'HTTP-404' %}
+<a class="btn" href="/admin/observability/errors?error_id={{ error_id }}">Ver detalle admin</a>
+{% endif %}

*** Update File: templates/admin_observability.html
@@
+<a class="btn" href="/admin/observability/errors">Errores</a>
+<div class="metric">{{ summary.traced_errors_total }}</div>
+{% for event in summary.latest_errors %}
+  <a class="mini-link" href="/admin/observability/errors?error_id={{ event.error_id }}">Ver detalle</a>
+{% endfor %}

*** Add File: templates/admin_observability_errors.html
+Vista admin para:
+- ultimos 100 errores
+- detalle por error_id
+- traceback completo
+- usuario, membresia, ip, referrer, user-agent

*** Update File: templates/home.html
@@
-{{ m.kickoff_time or m.live_depth.label }}
+{% set live_depth = m.live_depth|default({}) %}
+{{ m.kickoff_time or live_depth.get('label', 'Pendiente') }}

*** Update File: templates/calendar.html
@@
-{{ m.home_identity.crest_url }}
-{{ m.away_identity.crest_url }}
+{% set home_identity = m.home_identity|default({}) %}
+{% set away_identity = m.away_identity|default({}) %}
+{{ home_identity.get('crest_url', '') }}
+{{ away_identity.get('crest_url', '') }}

*** Update File: templates/live.html
@@
-{{ m.live_depth.minute }}
-{{ m.live_depth.label }}
-{{ m.home_identity.crest_url }}
-{{ m.away_identity.crest_url }}
+{% set live_depth = m.live_depth|default({}) %}
+{% set home_identity = m.home_identity|default({}) %}
+{% set away_identity = m.away_identity|default({}) %}
+{{ live_depth.get('minute', '-') }}
+{{ live_depth.get('label', 'Pendiente') }}
+{{ home_identity.get('crest_url', '') }}
+{{ away_identity.get('crest_url', '') }}

*** Update File: templates/picks.html
@@
-{{ m.live_depth.badge }}
-{{ m.live_depth.label }}
-{{ m.home_identity.crest_url }}
-{{ m.away_identity.crest_url }}
+{% set live_depth = m.live_depth|default({}) %}
+{% set home_identity = m.home_identity|default({}) %}
+{% set away_identity = m.away_identity|default({}) %}
+{{ live_depth.get('badge', 'scheduled') }}
+{{ live_depth.get('label', 'Pendiente') }}
+{{ home_identity.get('crest_url', '') }}
+{{ away_identity.get('crest_url', '') }}
*** End Patch
