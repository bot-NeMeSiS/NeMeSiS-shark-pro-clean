# V550 — Smart Dashboard + Zero Click Experience

Avance centrado en reducir navegación y mostrar al cliente lo importante al entrar.

## Incluye
- Dashboard inteligente al entrar si el usuario está logueado.
- Ruta `/dashboard` y alias `/inicio`.
- APIs `/api/dashboard/smart-summary` y `/api/client/today-focus`.
- Bloque “Lo importante hoy”.
- SHARK contextual visible en dashboard.
- Picks, favoritos, próximos partidos y recomendaciones en una pantalla.
- CSS premium responsive.

## QA
- `app.py` compila OK.
- Mantiene base completa V549.
- No incluye `.git`, DB local, logs ni cachés.
