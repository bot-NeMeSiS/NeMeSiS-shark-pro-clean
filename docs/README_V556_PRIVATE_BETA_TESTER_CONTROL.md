# V556 — Private Beta Tester Control

Avance orientado a preparar NeMeSiS SHARK PRO para testers reales.

Incluye:
- `/beta` para clientes/testers.
- `/admin/beta-center` para admin.
- tablas `beta_testers` y `beta_feedback`.
- APIs `/api/beta/status`, `/api/beta/join`, `/api/admin/beta-summary`, `/api/system/v556-check`.
- checklist beta y feedback persistente.
- mantiene V555 completo.

Render-ready.
