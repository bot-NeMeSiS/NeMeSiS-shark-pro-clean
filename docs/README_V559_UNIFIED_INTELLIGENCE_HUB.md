# V559 — Unified Intelligence Hub

Avance centrado en consolidar el producto:

- Cliente: nuevo `/intelligence-hub` con live, picks, recomendaciones, favoritos y próximos partidos en una vista.
- Admin: nuevo `/admin/unified-intelligence` con score global y accesos a usuarios, datos, picks, recomendaciones y Telegram.
- APIs: `/api/unified-intelligence/client`, `/api/unified-intelligence/admin`, `/api/system/v559-check`.
- Navegación cliente/admin más limpia y menos dispersa.
- Mantiene todo lo anterior y evita añadir pantallas sueltas sin control.

Build limpia Render-ready.
